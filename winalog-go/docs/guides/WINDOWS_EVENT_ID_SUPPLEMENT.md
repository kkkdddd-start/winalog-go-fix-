# Windows 事件 ID 补充手册

**项目**: WinLogAnalyzer-Go  
**版本**: v2.1  
**日期**: 2026-05-08  
**说明**: 本文档补充 `WINDOWS_EVENT_ID.md` 中描述过于简单的事件 ID，确保所有重要事件都有完整的字段说明和应急场景。

---

## 一、安全日志补充

### 4673 - 特权服务请求

**级别**: 审核成功  
**描述**: 账户请求了特权服务操作。此事件在用户尝试执行需要特定权限的系统级操作时生成，通常与 4674（特权对象操作）配对出现。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserSid | 请求特权的账户 SID | 确认操作者 |
| SubjectUserName | 账户名 | 非预期账户需告警 |
| SubjectLogonId | 登录会话 ID | 关联 4624 登录事件 |
| PrivilegeList | 请求的特权列表 | 分析请求的权限类型 |
| Service | 请求的服务名称 | 识别被操作的服务 |

**应急场景**:
- **提权检测**: 非管理员账户请求 SeDebugPrivilege → 可能是提权攻击
- **服务篡改**: 请求 SeLoadDriverPrivilege + 服务操作 → 可能是恶意驱动加载
- **权限滥用**: 普通用户频繁请求特权服务 → 账户已被攻陷
- **关联分析**: 4673 → 4674 序列表示特权服务请求后执行了特权操作

---

### 4674 - 对特权对象操作

**级别**: 审核成功  
**描述**: 对具有特殊权限的对象执行了操作。此事件在用户尝试修改、读取或删除受保护的系统对象时生成，是检测权限滥用和横向移动的重要指标。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ObjectServer | 对象服务器 (Security/LSA 等) | 识别操作的目标系统 |
| ObjectType | 对象类型 (File/Key/Token 等) | 分析操作意图 |
| ObjectName | 对象名称/路径 | 确认被操作的对象 |
| OperationType | 操作类型 (%%1536=创建, %%1537=读取, %%1538=写入, %%1539=删除, %%1540=执行) | 分析具体操作 |
| AccessMask | 请求的访问权限 (0x1=读, 0x2=写, 0x4=执行, 0x8=删除) | 评估操作严重性 |
| SubjectUserName | 执行操作的账户 | 确认操作者 |

**AccessMask 常见值**:

| 值 | 权限 | 说明 |
|----|------|------|
| 0x00000001 | FILE_READ_DATA | 读取文件数据 |
| 0x00000002 | FILE_WRITE_DATA | 写入文件数据 |
| 0x00000004 | FILE_APPEND_DATA | 追加文件数据 |
| 0x00000020 | FILE_EXECUTE | 执行文件 |
| 0x00010000 | DELETE | 删除对象 |
| 0x00040000 | WRITE_DAC | 修改访问控制 |
| 0x00080000 | WRITE_OWNER | 修改所有者 |

**应急场景**:
- **凭据窃取**: 访问 lsass.exe 进程 (ObjectType=Process, AccessMask=0x10) → 可能是 Mimikatz
- **SAM 数据库访问**: 访问 \Device\HarddiskVolume*\Windows\System32\config\SAM → 可能是密码提取
- **注册表篡改**: 修改 HKLM\SYSTEM\CurrentControlSet\Services → 可能是服务持久化
- **权限变更**: WRITE_DAC 或 WRITE_OWNER 操作 → 攻击者修改对象权限
- **数据外泄**: 大量读取操作指向敏感文件 (NTDS.dit, 密码文件)

---

### 4689 - 进程退出

**级别**: 审核成功  
**描述**: 进程已退出。此事件与 4688（进程创建）配对使用，用于追踪进程的完整生命周期。注意：此事件需要启用"审核进程终止"审核策略。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserSid | 退出进程的账户 SID | 确认进程所有者 |
| SubjectUserName | 账户名 | 非系统账户退出需关注 |
| SubjectLogonId | 登录会话 ID | 关联用户会话 |
| ProcessId | 进程 PID | 关联 4688 的 NewProcessId |
| NewProcessName | 退出的进程路径 | 识别退出的程序 |
| ExitStatus | 退出状态码 (0=正常) | 非零退出码表示异常 |

**ExitStatus 常见值**:

| 值 | 说明 |
|----|------|
| 0 | 正常退出 (STATUS_SUCCESS) |
| 1 | 通用错误 |
| 0xC0000005 | 访问违规 (Access Violation) |
| 0xC000013A | 被 Ctrl+C 终止 |
| 0xC0000409 | 堆栈缓冲区溢出 |

**应急场景**:
- **异常退出**: 关键系统进程 (如 svchost.exe, lsass.exe) 非零退出 → 可能是崩溃或攻击
- **进程生命周期**: 4688 → ... → 4689 可计算进程运行时长
- **恶意软件检测**: 短生命周期进程 (几秒内 4688→4689) + 异常路径 → 可能是 Dropper
- **进程替换**: 正常进程退出后立即出现同名新进程 → 可能是进程注入后重启

---

### 4722 - 账户已启用

**级别**: 审核成功  
**描述**: 禁用的用户账户被重新启用。此事件仅在域控制器上生成（域账户）或在本地计算机上生成（本地账户）。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行启用操作的管理员账户 | 确认操作者 |
| TargetUserName | 被启用的账户 | 确认目标账户 |
| TargetDomainName | 账户所在域 | 域/本地账户 |

**应急场景**:
- **休眠账户激活**: 长期禁用的管理员账户突然被启用 → 可能是攻击者重新激活休眠后门
- **离职员工账户**: 已离职员工的账户被启用 → 内部威胁或违规操作
- **关联检测**: 4722 → 4624 (短时间内) → 启用后立即登录，可能是攻击者在利用
- **合规审计**: 所有账户启用操作都应记录并审批

---

### 4723 - 尝试更改账户密码

**级别**: 审核成功/失败  
**描述**: 用户尝试更改自己的密码。此事件记录了密码更改的尝试，无论成功还是失败。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 尝试更改密码的账户 | 确认操作者 |
| TargetUserName | 密码被更改的账户 | 通常是同一账户 |
| Status | 状态码 (0x0=成功) | 分析结果 |

**常见 Status 值**:

| 值 | 说明 |
|----|------|
| 0x0 | 密码更改成功 |
| 0xC000006A | 旧密码不正确 |
| 0xC0000071 | 密码已过期 |
| 0xC000038D | 密码不满足复杂度要求 |

**应急场景**:
- **攻击者密码更改**: 管理员账户密码被非预期用户更改 → 账户已被攻陷
- **密码策略绕过**: 多次 4723 失败 → 可能是攻击者尝试设置弱密码
- **关联检测**: 4624 (登录) → 4723 (改密) → 可能是攻击者在锁定账户访问权

---

### 4725 - 账户已禁用

**级别**: 审核成功  
**描述**: 用户账户被禁用。此事件在管理员禁用账户时生成，是安全事件响应中的重要指标。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行禁用操作的管理员 | 确认操作者 |
| TargetUserName | 被禁用的账户 | 确认目标账户 |
| TargetDomainName | 账户所在域 | 域/本地账户 |

**应急场景**:
- **应急响应禁用**: 检测到入侵后管理员禁用被攻陷账户 → 正常操作
- **攻击者禁用**: 攻击者禁用其他管理员账户以阻止响应 → 严重告警
- **内部威胁**: 离职前员工账户被禁用 → 标准流程
- **频繁禁用/启用**: 同一账户反复 4725 → 4722 → 可能是配置问题或攻击

---

### 4726 - 账户已删除

**级别**: 审核成功  
**描述**: 用户账户被删除。此事件在永久删除账户时生成，是不可逆操作。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行删除操作的管理员 | 确认操作者 |
| TargetUserName | 被删除的账户 | 确认被删除的账户 |
| TargetDomainName | 账户所在域 | 域/本地账户 |

**应急场景**:
- **攻击者清理**: 入侵后删除临时创建的后门账户 → 4720 (创建) → 使用 → 4726 (删除)
- **内部威胁**: 大量账户被删除 → 可能是恶意内部人员
- **合规审计**: 所有账户删除操作都应记录原因和审批
- **关联检测**: 4726 前有 4624 (登录) → 可能是攻击者使用后删除痕迹

---

### 4733 - 从安全启用型本地组移除

**级别**: 审核成功  
**描述**: 成员从安全启用型本地组中移除。此事件与 4732 配对，记录本地组成员移除操作。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行移除的管理员 | 确认操作者 |
| TargetUserName | 目标组名 | 确认从哪个组移除 |
| MemberSid | 被移除的成员 | 确认谁被移除 |

**应急场景**:
- **权限回收**: 检测到入侵后将攻陷账户从本地管理员组移除
- **痕迹清理**: 攻击者使用后门将账户从本地组移除

---

### 4735 - 安全启用型本地组已更改

**级别**: 审核成功  
**描述**: 安全启用型本地组的属性被修改。此事件记录本地组的名称、描述或其他属性的变更。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行修改的管理员 | 确认操作者 |
| TargetUserName | 被修改的组名 | 确认目标组 |
| PrivilegeList | 修改的特权列表 | 分析权限变更 |

**应急场景**:
- **组属性篡改**: 修改组描述以隐藏恶意目的 → 社会工程
- **权限变更**: 修改组的权限设置 → 可能是攻击者放宽限制

---

### 4737 - 安全启用型全局组已更改

**级别**: 审核成功  
**描述**: 安全启用型全局组的属性被修改。此事件记录全局组的名称、描述或其他属性的变更。

**应急场景**:
- **组属性篡改**: 修改组描述以隐藏恶意目的
- **关联检测**: 4737 → 4728 (添加成员) → 可能是为添加成员做准备

---

### 4738 - 用户账户已更改

**级别**: 审核成功  
**描述**: 用户账户的属性被修改。此事件记录账户名称、描述、主组、配置文件路径等属性的变更。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行修改的管理员 | 确认操作者 |
| TargetUserName | 被修改的账户 | 确认目标账户 |
| DisplayName | 显示名称 | 显示名称被修改可能是社会工程 |
| UserPrincipalName | 用户主体名称 | UPN 修改可能用于欺骗 |
| HomeDirectory | 主目录 | 主目录修改可能是数据窃取路径 |
| ProfilePath | 配置文件路径 | 配置文件路径修改可能是持久化 |
| UserAccountControl | 账户控制标志 | 账户控制标志变更需关注 |

**UserAccountControl 常见标志**:

| 值 | 标志 | 说明 |
|----|------|------|
| 0x0001 | ACCOUNTDISABLE | 账户已禁用 |
| 0x0010 | LOCKOUT | 账户已锁定 |
| 0x0020 | PASSWD_NOTREQD | 不需要密码 |
| 0x0200 | NORMAL_ACCOUNT | 普通用户账户 |
| 0x1000 | WORKSTATION_TRUST_ACCOUNT | 工作站信任账户 |
| 0x2000 | SERVER_TRUST_ACCOUNT | 服务器信任账户 (域控) |

**应急场景**:
- **账户描述篡改**: 攻击者修改账户描述以伪装成正常账户
- **权限变更**: 修改 UserAccountControl 标志启用/禁用账户或设置密码策略
- **关联检测**: 4738 → 4624 (登录) → 可能是修改后立即使用账户

---

### 4741 - 计算机账户已创建

**级别**: 审核成功  
**描述**: 创建了新的计算机账户。此事件在域控制器上生成，当新计算机加入域时自动创建。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行创建的管理员/系统 | 确认操作者 |
| TargetUserName | 新计算机账户名 (以 $ 结尾) | 确认新加入的计算机 |
| TargetSid | 新计算机 SID | 唯一标识 |

**应急场景**:
- **异常计算机加入**: 非预期的计算机账户创建 → 可能是攻击者加入恶意主机
- **数量异常**: 短时间内大量计算机账户创建 → 可能是自动化攻击
- **关联检测**: 4741 → 4624 (计算机账户登录) → 新计算机加入后立即认证

---

### 4742 - 计算机账户已更改

**级别**: 审核成功  
**描述**: 计算机账户的属性被修改。此事件在域控制器上生成，记录计算机账户的 DNS 名称、OS 版本、服务主体等属性的变更。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行修改的管理员/系统 | 确认操作者 |
| TargetUserName | 被修改的计算机账户 | 确认目标计算机 |
| DNSHostName | DNS 主机名 | DNS 名称变更可能是欺骗 |
| OperatingSystem | 操作系统版本 | OS 版本变更可能是系统替换 |

**应急场景**:
- **计算机属性篡改**: 修改计算机 DNS 名称 → 可能是网络欺骗
- **OS 版本变更**: 操作系统版本字段异常变更 → 可能是系统被替换

---

### 4743 - 计算机账户已删除

**级别**: 审核成功  
**描述**: 计算机账户被删除。此事件在域控制器上生成，当计算机从域中移除时触发。

**应急场景**:
- **异常计算机移除**: 合法计算机账户被删除 → 可能是攻击者破坏域信任关系
- **清理操作**: 退役计算机的账户删除 → 正常操作

---

### 4770 - TGS 更新

**级别**: 审核成功  
**描述**: Kerberos TGS (Ticket Granting Service) 票据被更新。当客户端请求延长现有 TGS 票据的生命周期时生成此事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| TargetUserName | 目标账户 | 确认票据续期的用户 |
| ServiceName | 服务名称 | 确认续期的服务票据 |
| TicketOptions | 票据选项 | 分析票据特性 |
| Status | 状态码 | 0x0=成功 |

**应急场景**:
- **异常频繁续期**: 同一账户短时间内大量 TGS 更新 → 可能是自动化攻击工具
- **长期会话**: TGS 频繁续期保持长期访问 → 可能是持久化攻击
- **服务票据续期**: 高权限服务票据续期 → 可能是攻击者保持访问权

---

### 4772 - TGT 请求失败

**级别**: 审核失败  
**描述**: Kerberos TGT (Ticket Granting Ticket) 请求失败。此事件在域控制器上生成，当 KDC 无法颁发 TGT 时触发。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| TargetUserName | 请求的账户 | 确认哪个账户请求失败 |
| TargetDomainName | 目标域 | 确认请求的域 |
| Status | 失败状态码 | 分析失败原因 |
| IpAddress | 来源 IP | 定位请求来源 |

**常见 Status 值**:

| 值 | 说明 |
|----|------|
| 0x6 | 用户名不存在 |
| 0x12 | 账户已吊销 |
| 0x17 | 密码已过期 |
| 0x18 | 预认证失败 (密码错误) |

**应急场景**:
- **暴力破解**: 大量 4772 来自同一 IP → 可能是 Kerberos 暴力破解
- **账户枚举**: 针对不同用户名的 4772 (Status=0x6) → 可能是账户枚举攻击
- **黄金票据检测**: 异常的 TGT 请求失败模式 → 可能是伪造票据尝试

---

### 4773 - TGS 请求失败

**级别**: 审核失败  
**描述**: Kerberos TGS (Ticket Granting Service) 请求失败。此事件在域控制器上生成，当 KDC 无法颁发服务票据时触发。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| TargetUserName | 请求的账户 | 确认哪个账户请求失败 |
| ServiceName | 请求的服务 | 确认请求哪个服务的票据 |
| Status | 失败状态码 | 分析失败原因 |

**应急场景**:
- **Kerberoasting 失败**: 攻击者尝试请求服务票据但失败 → 可能是目标服务不存在
- **服务枚举**: 针对不同服务的 4773 → 可能是攻击者在枚举可用服务
- **权限不足**: 账户无权请求特定服务票据 → 可能是权限配置问题

---

### 4774 - 账户登录信息显示

**级别**: 审核成功  
**描述**: 账户映射请求成功。此事件在域控制器上生成，当账户信息被查询用于登录目的时触发。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| TargetUserName | 被查询的账户 | 确认哪个账户被查询 |
| TargetDomainName | 账户所在域 | 确认查询的域 |

**应急场景**:
- **账户枚举**: 大量 4774 查询不同账户 → 可能是攻击者在枚举域账户
- **关联检测**: 4774 → 4768 (TGT 请求) → 查询后立即请求票据，可能是正常登录流程
- **异常查询模式**: 非工作时间的大量账户查询 → 可能是攻击行为

---

### 4658 - 关闭对象句柄

**级别**: 审核成功  
**描述**: 对象句柄被关闭。此事件与 4656（请求对象句柄）和 4663（尝试访问对象）配对使用，用于追踪完整的对象访问生命周期。

**应急场景**:
- **会话结束**: 大量句柄关闭 → 可能是用户注销或进程终止
- **关联分析**: 4656 → 4663 → 4658 表示完整的对象访问周期

---

### 4660 - 删除对象

**级别**: 审核成功  
**描述**: 对象被删除。此事件在文件、注册表键或其他系统对象被永久删除时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ObjectName | 被删除的对象名称 | 确认被删除的文件/注册表键 |
| ObjectType | 对象类型 (File, Key 等) | 分析删除类型 |

**应急场景**:
- **痕迹清理**: 攻击者删除日志文件或恶意工具 → 4660 是反取证指标
- **数据破坏**: 大量文件删除 → 可能是勒索软件或破坏性攻击
- **关联检测**: 4663 (访问) → 4660 (删除) → 先访问后删除

---

### 4662 - 对象操作

**级别**: 审核成功  
**描述**: 对目录服务对象执行了操作。此事件主要在 Active Directory 域控制器上生成，记录 AD 对象的创建、修改、删除等操作。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ObjectServer | 对象服务器 (通常为 DS) | 确认操作的目标系统 |
| ObjectType | 对象类型 (User, Computer, Group 等) | 分析操作类型 |
| ObjectName | 对象名称 | 确认被操作的对象 |
| OperationType | 操作类型 | 分析具体操作 |

**应急场景**:
- **AD 对象修改**: 非预期的 AD 对象变更 → 可能是攻击者在修改域配置
- **关联检测**: 4662 → 4720 (账户创建) → 可能是 AD 账户创建过程

---

### 4663 - 尝试访问对象

**级别**: 审核成功/失败  
**描述**: 尝试访问对象（文件、注册表键等）。此事件在启用"审核对象访问"策略后生成，是检测数据窃取和权限滥用的关键事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 访问账户 | 确认操作者 |
| ObjectName | 对象路径 | 确认被访问的文件/注册表 |
| ObjectType | 对象类型 (File, Key, Process) | 分析访问类型 |
| AccessMask | 请求的访问权限 | 评估操作意图 |
| AccessList | 具体权限列表 | %%4416=读, %%4417=写, %%4418=执行, %%4419=删除 |

**应急场景**:
- **数据窃取**: 访问敏感文件 (数据库、备份、NTDS.dit)
- **权限变更**: 修改文件权限 → 可能是攻击者提升访问权
- **注册表篡改**: 访问关键注册表路径 → 可能是持久化操作
- **进程访问**: 访问 lsass.exe 等系统进程 → 可能是凭据窃取

---

### 4670 - 对象权限更改

**级别**: 审核成功  
**描述**: 对象的权限描述符（DACL/SACL）被修改。此事件在文件、注册表键、服务等对象的访问控制列表被修改时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 执行修改的账户 | 确认操作者 |
| ObjectName | 对象路径 | 确认被修改的对象 |
| ObjectType | 对象类型 | 文件/注册表/服务等 |
| SecurityDescriptor | 新的安全描述符 | 分析权限变更内容 |

**应急场景**:
- **权限提升**: 修改文件权限允许非管理员访问 → 可能是攻击者放宽限制
- **审计规避**: 删除 SACL (System ACL) → 可能是攻击者关闭审核
- **持久化**: 修改服务权限 → 可能是攻击者确保服务不被停止
- **数据窃取**: 修改文件权限允许读取敏感数据 → 可能是数据外泄准备

---

### 5141 - 网络共享访问详细

**级别**: 审核成功  
**描述**: 网络共享访问的详细信息。此事件与 5140 配对，提供更详细的共享访问信息，包括访问的具体文件路径和访问权限。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 访问账户 | 确认操作者 |
| ShareName | 共享名称 | 确认被访问的共享 |
| RelativeTargetName | 相对目标路径 | 确认访问的具体文件 |
| AccessMask | 访问权限 | %%4416=读, %%4417=写, %%4418=执行, %%4419=删除 |
| IpAddress | 来源 IP | 定位访问来源 |

**应急场景**:
- **数据外泄**: 访问大量文件 + 写权限 → 可能是数据被窃取或篡改
- **横向移动**: 通过管理共享 (admin$, c$) 访问远程主机 → 攻击链路
- **关联检测**: 5140 → 5141 → 5145 表示完整的 SMB 访问过程

---

### 5142 - SMB 会话设置

**级别**: 审核成功  
**描述**: SMB 会话被设置。此事件在 SMB 连接建立时生成，记录会话协商的详细信息。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 发起连接的账户 | 确认操作者 |
| ShareName | 共享名称 | 确认被访问的共享 |
| IpAddress | 来源 IP | 定位访问来源 |
| SessionId | 会话 ID | 关联其他 SMB 事件 |

**应急场景**:
- **SMB 会话监控**: 跟踪所有 SMB 连接的建立
- **异常连接**: 来自外部 IP 的 SMB 会话 → 可能是攻击

---

### 5143 - SMB 会话断开

**级别**: 审核成功  
**描述**: SMB 会话被断开。此事件与 5142 配对，用于追踪 SMB 会话的完整生命周期。

**应急场景**:
- **会话时长**: 5142 → 5143 可计算 SMB 会话持续时间
- **异常断开**: 正常会话意外断开 → 可能是网络问题或攻击

---

### 5144 - SMB 会话重新连接

**级别**: 审核成功  
**描述**: SMB 会话被重新连接。此事件在 SMB 连接因网络问题断开后重新建立时生成。

**应急场景**:
- **网络问题**: 频繁的 5143 → 5144 循环 → 可能是网络不稳定
- **攻击特征**: 重连模式异常 → 可能是攻击工具特性

---

### 1104 - 安全日志记录已满

**级别**: 信息  
**描述**: 安全日志达到最大配置大小。此事件在日志文件满且覆盖策略为"不覆盖"时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| MaxSize | 日志最大大小 | 确认日志配置 |
| CurrentSize | 当前日志大小 | 确认日志已满 |

**应急场景**:
- **攻击者痕迹隐藏**: 攻击者可能故意填满日志以掩盖后续活动 → 严重告警
- **配置问题**: 日志大小配置过小 → 需要调整日志策略
- **合规要求**: 日志满后可能丢失关键安全事件 → 违反合规要求
- **关联检测**: 1104 后无新安全事件 → 可能是日志已满停止记录

---

### 4719 - 审核策略已更改

**级别**: 审核成功  
**描述**: 系统审核策略被修改。此事件在本地或组策略中的审核设置被更改时生成，是检测攻击者降低安全监控能力的关键指标。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| SubjectUserName | 修改策略的账户 | 确认操作者 |
| SubjectDomainName | 账户所在域 | 域/本地操作 |
| AuditPolicyChanges | 变更内容 | %%8448=已添加, %%8449=已删除, %%8450=已修改 |
| CategoryId | 审核类别 GUID | 确认哪个审核类别被修改 |
| CategoryName | 审核类别名称 | 如"账户登录"、"对象访问"等 |
| SubcategoryName | 子类别名称 | 更具体的审核项 |

**常见审核类别**:

| 类别 | 说明 | 风险 |
|------|------|------|
| 账户登录 | 登录/注销事件 | 中 |
| 账户管理 | 账户创建/修改/删除 | 高 |
| 对象访问 | 文件/注册表访问 | 中 |
| 进程创建 | 新进程启动 | 高 |
| 策略更改 | 审核策略修改 | 极高 |
| 特权使用 | 特权服务请求 | 高 |

**应急场景**:
- **攻击者关闭审核**: 攻击者禁用"进程创建"或"账户登录"审核以隐藏活动 → 严重告警
- **持久化**: 修改审核策略确保长期不被检测 → 攻击链路关键步骤
- **关联检测**: 4624 (登录) → 4719 (修改审核) → 攻击者登录后降低监控
- **合规审计**: 所有审核策略变更都应记录并审批

---

## 二、系统日志补充

### 7030 - 安全权限警告

**级别**: 警告  
**描述**: 服务配置的安全权限不是最佳实践。此事件在服务的安全描述符配置不当时生成，可能是权限过宽或过窄。

**应急场景**:
- **权限过宽**: 服务权限允许非管理员启动/停止 → 可能是攻击者利用
- **配置审计**: 定期检查服务权限配置是否符合最小权限原则

---

### 7031 - 服务意外终止

**级别**: 错误  
**描述**: 服务意外终止。此事件在服务非正常停止时生成，通常伴随服务崩溃或被强制终止。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ServiceName | 服务名称 | 确认哪个服务终止 |
| TerminationType | 终止类型 | 崩溃/被终止/超时 |
| Action | 后续操作 | 重启/不操作 |

**应急场景**:
- **攻击**: 服务被攻击者手动终止 → 可能是禁用安全软件
- **检测**: 分析服务终止前的其他异常事件 (如 4688 进程创建)
- **关键服务**: 安全软件服务终止 → 立即调查

---

### 7032 - 服务控制管理器错误

**级别**: 错误  
**描述**: 服务控制管理器无法处理启动失败。此事件在服务启动失败且服务控制管理器无法自动修复时生成。

**应急场景**:
- **服务故障**: 关键服务启动失败 → 系统功能受损
- **攻击特征**: 攻击者修改服务配置导致启动失败 → 可能是破坏系统

---

### 7033 - 服务进入故障恢复状态

**级别**: 信息  
**描述**: 服务进入故障恢复状态。此事件在服务配置了恢复操作且正在执行恢复时生成。

**应急场景**:
- **服务恢复**: 监控关键服务的自动恢复状态
- **恢复失败**: 恢复操作失败 → 需要手动干预

---

### 7034 - 服务意外退出

**级别**: 错误  
**描述**: 服务意外退出。此事件在服务非正常退出时生成，与 7031 类似但更侧重于进程级别的退出。

**应急场景**:
- **异常退出**: 安全软件或关键服务意外退出 → 可能是攻击行为
- **关联检测**: 7034 → 4688 (新进程) → 可能是服务被替换

---

### 7035 - 发送控制到服务

**级别**: 信息  
**描述**: 发送控制代码到服务。此事件在服务控制管理器向服务发送启动、停止、暂停等控制代码时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ServiceName | 服务名称 | 确认哪个服务被控制 |
| ControlCode | 控制代码 | %%1=停止, %%2=暂停, %%3=继续, %%4=查询状态 |
| UserName | 发送控制的用户 | 确认操作者 |

**应急场景**:
- **异常控制**: 非管理员用户发送服务控制代码 → 可能是权限提升
- **服务停止**: 安全软件被停止 → 立即调查
- **关联检测**: 7035 (停止) → 7036 (服务停止) → 完整的停止过程

---

### 7036 - 服务状态更改

**级别**: 信息  
**描述**: 服务状态已更改（启动、停止、暂停、继续）。此事件是服务状态变化的通用事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ServiceName | 服务名称 | 确认哪个服务 |
| NewState | 新状态 | 运行/停止/暂停/继续 |

**应急场景**:
- **监控**: 监控关键服务状态变更
- **异常**: 非预期的服务停止 → 可能是攻击或故障
- **关联检测**: 7045 (服务创建) → 7036 (服务启动) → 新服务启动过程

---

### 7037 - 服务权限警告

**级别**: 警告  
**描述**: 服务权限不足以访问资源。此事件在服务尝试访问其权限不足的资源时生成。

**应急场景**:
- **权限不足**: 服务无法访问必要资源 → 可能是配置错误或攻击
- **服务故障**: 关键服务因权限问题无法正常工作

---

### 7040 - 服务启动类型更改

**级别**: 信息  
**描述**: 服务的启动类型已更改（自动、手动、禁用）。此事件在服务配置被修改时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ServiceName | 服务名称 | 确认哪个服务 |
| OldStartType | 旧启动类型 | 自动/手动/禁用 |
| NewStartType | 新启动类型 | 自动/手动/禁用 |

**应急场景**:
- **持久化**: 恶意服务从手动改为自动 → 攻击者确保服务重启后运行
- **安全软件禁用**: 安全软件从自动改为禁用 → 攻击者降低监控
- **关联检测**: 7040 (改为自动) → 7036 (服务启动) → 服务被启用并启动

---

### 6005 - 事件日志服务已启动

**级别**: 信息  
**描述**: 事件日志服务成功启动。此事件在系统启动或事件日志服务重新启动时生成，表示系统开始记录事件。

**应急场景**:
- **系统重启**: 结合其他事件分析系统运行时间
- **异常重启**: 非预期时间的 6005 → 可能是系统被重启（攻击或维护）
- **日志连续性**: 6005 → 6006 表示完整的日志服务周期

---

### 6006 - 事件日志服务已停止

**级别**: 信息  
**描述**: 事件日志服务正常停止。此事件在系统正常关机或事件日志服务被停止时生成。

**应急场景**:
- **正常关机**: 6006 后有 1074 (计划关机) → 正常关机流程
- **异常**: 无 6006 直接断电 → 可能丢失未写入的日志

---

### 6008 - 意外关闭

**级别**: 错误  
**描述**: 系统意外关闭。此事件在系统未正常关机就断电或崩溃时生成，下一次启动时记录。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| PreviousShutdownTime | 上次关机时间 | 确认异常关机时间 |
| PreviousShutdownDate | 上次关机日期 | 日期信息 |

**应急场景**:
- **系统崩溃**: BSOD 后重启 → 6008 表示上次是异常关机
- **电源故障**: 断电导致异常关机 → 硬件问题
- **攻击**: 攻击者强制重启系统 → 可能是攻击后清理

---

### 41 - 系统意外重启

**级别**: 关键  
**描述**: 系统在没有正确关机的情况下重启。此事件在 Kernel-Power 日志中生成，是检测系统异常重启的关键事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| BugcheckCode | 蓝屏错误代码 | 0=非 BSOD, 其他=BSOD 代码 |
| BugcheckParameter1-4 | 蓝屏参数 | BSOD 详细分析 |
| SleepInProgress | 睡眠状态 | 0=非睡眠, 1=睡眠中 |
| PowerButtonTimestamp | 电源按钮时间戳 | 物理按钮操作时间 |

**可能原因**:

| BugcheckCode | 说明 | 应急场景 |
|-------------|------|----------|
| 0 | 非 BSOD (断电/硬重启) | 电源故障或强制重启 |
| 0x1A | MEMORY_MANAGEMENT | 内存故障或驱动问题 |
| 0x3B | SYSTEM_SERVICE_EXCEPTION | 驱动或系统文件损坏 |
| 0x50 | PAGE_FAULT_IN_NONPAGED_AREA | 内存访问违规 |
| 0x7E | SYSTEM_THREAD_EXCEPTION_NOT_HANDLED | 驱动异常 |
| 0x9F | DRIVER_POWER_STATE_FAILURE | 驱动电源状态问题 |
| 0xEF | CRITICAL_PROCESS_DIED | 关键进程死亡 |

**应急场景**:
- **电源故障**: BugcheckCode=0 → 可能是断电或硬重启
- **系统崩溃**: BugcheckCode!=0 → BSOD 分析
- **远程攻击**: 攻击者远程重启系统 → 可能是攻击后清理
- **关联检测**: 41 (重启) → 6005 (日志启动) → 完整的重启过程

---

## 三、Sysmon 事件补充

### EventID 4 - Sysmon 服务状态更改

**描述**: Sysmon 服务状态已更改。此事件在 Sysmon 服务启动、停止或配置更改时生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| State | 服务状态 (Started/Stopped) | 确认 Sysmon 状态 |
| Version | Sysmon 版本 | 确认使用的版本 |
| SchemaVersion | 配置架构版本 | 确认配置兼容性 |

**应急场景**:
- **Sysmon 停止**: 攻击者停止 Sysmon 以逃避检测 → 严重告警
- **配置更改**: Sysmon 配置被修改 → 可能是攻击者降低监控范围
- **关联检测**: EventID 16 (配置更改) → EventID 4 (状态更改) → 配置更新过程

---

### EventID 6 - Driver Loaded

**描述**: 内核驱动被加载到操作系统中。此事件在设备驱动加载时生成，是检测恶意驱动和 Rootkit 的关键事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ImageLoaded | 驱动文件路径 | 确认加载的驱动 |
| Hashes | 驱动哈希 (SHA256, MD5, IMP) | 恶意驱动识别 |
| Signed | 是否签名 (true/false) | 未签名驱动需高度关注 |
| Signature | 签名者 | 确认签名有效性 |

**应急场景**:
- **Rootkit 检测**: 未签名或签名无效的驱动 → 可能是 Rootkit
- **恶意驱动**: 来自非标准路径的驱动 → 可能是攻击者加载的恶意驱动
- **驱动劫持**: 合法驱动被替换 → 检测哈希变更

---

### EventID 9 - RawAccessRead

**描述**: 进程通过 \\.\ 设备路径读取原始磁盘。此事件在进程直接读取物理磁盘扇区时生成，是检测凭据转储和磁盘映像提取的关键事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| Image | 读取磁盘的进程路径 | 确认操作者 |
| Device | 读取的设备 (如 \\.\PhysicalDrive0) | 确认读取的磁盘 |

**应急场景**:
- **凭据转储**: 直接读取磁盘提取 SAM 数据库 → 可能是 Mimikatz 或类似工具
- **磁盘映像**: 创建磁盘映像用于离线分析 → 可能是攻击者窃取数据
- **取证工具**: 合法取证工具也会触发此事件 → 需要白名单管理

---

### EventID 15 - FileCreateStreamHash

**描述**: 创建文件时检测到 NTFS 替代数据流 (ADS)。此事件在文件创建时如果包含替代数据流则生成。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| Image | 创建文件的进程 | 确认操作者 |
| TargetFilename | 目标文件名 | 确认被创建的文件 |
| CreationUtcTime | 创建时间 | 时间分析 |
| Hash | 流内容哈希 | 恶意内容识别 |

**应急场景**:
- **恶意软件隐藏**: 攻击者使用 ADS 隐藏恶意代码 → 检测异常流
- **数据外泄**: 通过 ADS 隐藏外泄数据 → 检测大流文件

---

### EventID 15 - FileCreate

**描述**: 文件创建。此事件在文件被创建时生成，是检测恶意文件下载、持久化和数据外泄的关键事件。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| Image | 创建文件的进程路径 | 确认操作者 |
| TargetFilename | 创建的文件路径 | 确认创建的文件 |
| CreationUtcTime | 创建时间 (UTC) | 时间分析 |

**应急场景**:
- **恶意下载**: 浏览器或下载工具创建可执行文件 → 可能是恶意软件
- **持久化**: 在启动目录创建文件 → 可能是持久化机制
- **数据外泄**: 在共享目录创建压缩文件 → 可能是数据打包外泄
- **检测规则**: 创建来自非标准路径的 .exe、.dll、.ps1 文件 → 告警

---

## 四、PowerShell 事件补充

### 4103 - 模块日志

**描述**: PowerShell 模块执行。此事件在 PowerShell 模块加载或执行时生成，记录模块的详细执行信息。

**重要字段**:

| 字段 | 说明 | 应急场景 |
|------|------|----------|
| ContextInfo | 上下文信息 | 分析执行环境 |
| ScriptName | 脚本名称 | 确认执行的脚本 |
| ScriptPath | 脚本路径 | 识别脚本来源 |
| Command | 执行的命令 | 分析具体操作 |
| Member | 执行类型 | 确认执行方式 |

**应急场景**:
- **恶意模块**: 加载非标准 PowerShell 模块 → 可能是攻击工具
- **脚本来源**: 脚本来自网络路径或 Temp 目录 → 可能是下载的执行器
- **关联检测**: 4103 → 4104 (脚本块) → 模块加载后执行恶意代码

---

## 五、使用建议

1. **审核策略配置**: 确保启用相关审核策略以生成这些事件
2. **日志收集**: 使用 WinLogAnalyzer-Go 或其他 SIEM 工具收集和分析这些事件
3. **告警规则**: 基于应急场景创建告警规则
4. **白名单管理**: 为合法操作创建白名单以减少误报
5. **关联分析**: 将多个事件关联以检测完整的攻击链路

---

*本文档由 WinLogAnalyzer-Go 团队整理，作为 WINDOWS_EVENT_ID.md 的补充参考。*
